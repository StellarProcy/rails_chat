= simple_form_for [:admin, @admin], html: { class: 'form-group' } do |f|
    = f.input :email
    = f.input :password
    = f.submit class: 'btn btn-primary'
    = link_to 'Cancel', [:admin, :admins], class: 'btn'
